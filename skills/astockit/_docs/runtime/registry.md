# Registry

The bundle ships three machine-readable registry files:

- `_registry/bundle.json`
- `_registry/skills.json`
- `_registry/runtime.json`

They should be read directly as bundled metadata rather than through a unified launcher.
